#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    int n,exponent,res;
    cout<<"enter the value of n,exponent"<<endl;
    cin>>n>>exponent;
    res=pow(n,exponent);
    cout<<"result="<<res<<endl;
}
