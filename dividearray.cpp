#include <bits/stdc++.h>
using namespace std;
int main ()
{
 int arr[30];
 int i, n, a, j;
 cin>>n;
 for (i = 0; i < n; ++i)
    cin>>arr[i]);
    cin>>a;
 for (i = 0; i < a; ++i)
 {
   arr[n] = arr[0];
   for (j = 0; j < n; ++j) 
    number[j] = number[j + 1];
 }

for (i = 0; i < n; ++i)
    cout<<arr[i];
}
